{% extends 'base.html.twig' %}

{% block title %}Edit PersonalData{% endblock %}

{% block body %}
    <h1>Edit PersonalData</h1>

    {{ include('personal_data/_form.html.twig', {'button_label': 'Update'}) }}
    {% if not is_granted('ROLE_USER')%}
        <a href="{{ path('personal_data_index') }}">back to list</a>\
    {% endif %}
    {% if not is_granted('ROLE_USER')%}
        {{ include('personal_data/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
