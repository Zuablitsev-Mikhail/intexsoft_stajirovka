{% extends 'base.html.twig' %}

{% block title %}ProjectRoles{% endblock %}

{% block body %}
    <h1>ProjectRoles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_role.id }}</td>
            </tr>
            <tr>
                <th>title</th>
                <td>{{ project_role.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('project_roles_index') }}">back to list</a>

    <a href="{{ path('project_roles_edit', {'id': project_role.id}) }}">edit</a>

    {{ include('project_roles/_delete_form.html.twig') }}
{% endblock %}
